# 상대값 기반 측정 실험 계획

**목적:** 비싼 계측 장비 없이, 캘리브레이션과 **독립된** 방법으로 우리 시스템의 오차를 측정한다.

작성일 2026-09-17. 대상 실험 3개 — **A) 스케일**, **B) flange↔cube 변환**, **C) 로봇 정밀도**.

---

## 0. 왜 이 문서가 필요한가

지금 우리에게 세 가지 열린 문제가 있다.

| 문제 | 현재 상태 | 리뷰어 판정 |
|---|---|---|
| 마커 인쇄 스케일 0.99 | **캘리퍼스 실측 없이 train RMSE로 튜닝해서 골랐다** | 치명적 |
| z 공통편향 +2.2 mm | 원인 **추정만** 하고 판정 못 함 | 주요 |
| GT의 T_gripper_cube | **비전으로 적합**해서 GT가 비전과 독립이 아님 | 치명적 |

셋 다 **절대 기준(GT)이 없어서** 생긴 문제다. 그런데 절대 기준은 비싸다.

**이 문서의 전략:** 절대 기준 대신 **상대값**을 쓴다. 상대값은 공짜거나 매우 싸면서도, 위 세 문제 중 상당 부분을 직접 해결한다.

### 상대값이 왜 통하는가

로봇은 **"내가 정확히 어디 있나"는 잘 모르지만, "얼마나 움직였나"는 훨씬 정확하다.**

| 용어 | 뜻 | 우리 로봇 |
|---|---|---|
| **반복정밀도** | 같은 명령에 **매번 같은 양**만큼 움직이는 능력 | 데이터시트 ±0.03 mm (미검증) |
| **절대정확도** | 명령한 좌표에 **실제로** 가는 능력 | 보통 mm 단위, 훨씬 나쁨 |

**캘리브레이션 검증에는 반복정밀도만 있으면 됩니다.** 절대정확도는 필요 없다. 그래서 로봇 자체를 자로 쓸 수 있다.

### 상대값으로 못 잡는 것 (미리 밝힘)

**시스템 전체가 통째로 밀린 오차**는 상대값으로 절대 못 잡는다. 모든 상대량은 전역 강체변환에 불변이기 때문이다.

이걸 **공통 편향**(common-mode bias, 모든 측정이 같은 방향으로 함께 틀어진 오차)이라고 한다. 우리 z +2.2 mm가 여기 해당할 가능성이 크다 — 세 고정캠이 전부 +2.0~+2.4로 **같은 방향**이다.

공통 편향은 이 문서의 범위 밖이고, 별도로 **숫자 6개**(위치 3 + 회전 3)만 고정하면 된다. 중력 수준기와 로봇 base 기준물로 잡는다.

---

## 실험 A — 상대 이동 스위프 (마커 스케일)

### A.1 무엇을 아는가

**마커가 실제보다 1 % 작으면, 비전은 거리를 1 % 크게 본다.** 로봇이 300 mm 움직였는데 비전은 303 mm라고 한다. 3 mm 차이는 우리 측정 잡음(약 0.4 mm)보다 훨씬 커서 **확실히 검출된다.**

### A.2 원리

```
로봇이 큐브를 쥔 채 여러 위치로 이동하며 촬영
   ↓
비전이 본 이동량과 FK가 알려준 이동량을 회귀
```

말로 하면 "로봇이 움직인 만큼 비전도 움직였다고 하는가"이다. 식으로는 이렇다.

$$d_{\text{비전}} = a \cdot d_{\text{FK}} + b$$

| 나온 값 | 뜻 |
|---|---|
| **기울기 $a$** | **스케일 오차.** $a = 1.01$이면 비전이 1 % 크게 본다 |
| 절편 $b$ | 0점 차이. 여기선 관심 없음 |
| 잔차 산포 | 측정 잡음 |

**기울기는 로봇 절대정확도와 무관하다.** 반복정밀도만 관여한다.

### A.3 데이터는 이미 있다

| 세션 | 장수 | 내용 | 스위프에 쓸 수 있나 |
|---|---:|---|---|
| **session1** | **45** | **고정캠 3대 — 그리퍼로 쥔 큐브, 높이 다양** | ✅ **그대로 사용 가능** |
| session2 | 15 | 큐브를 놓고 배치 | ❌ 아래 참조 |
| session3 | 15 | 그리퍼캠만 | △ 실험 A' 참조 |

**session2를 쓰면 안 되는 이유 두 가지**

1. **z가 고정이다.** `session2_pick_and_place.py`가 `Z_FIXED = GRASP_REF_POSE[2] = 178.75`로 못박아, 15곳 배치가 **전부 같은 높이**다. x, y, rz만 변한다. 그런데 **우리 문제가 바로 z**다.
2. **릴리즈 슬립이 섞인다.** 큐브를 놓으므로 배치마다 0.77 mm 슬립이 잡음으로 들어간다.

**session1이 맞는 이유:** 큐브를 **쥔 채로** 움직이고(슬립 없음), **높이가 다양하며**(z 측정 가능), 45장으로 충분하다.

### A.4 계산 규칙 — 이걸 지켜야 독립이 된다

> **held-out 분리가 필요 없다.** 이건 모델 평가가 아니라 **물리 측정**이기 때문이다.
> 캘리퍼스로 마커를 재는 데 held-out이 필요 없는 것과 같다.
> 단, 아래 3개를 지켜야 "캘리브레이션을 안 쓴다"가 성립한다.

**규칙 1 — 단일 카메라 PnP와 거리만 쓴다**

| 금지 | 이유 |
|---|---|
| 여러 카메라 삼각측량 | camera extrinsic이 session1로 적합됨 → 순환 |
| $T^B_{C_i}$ (카메라 외부파라미터) | 같음 |
| $T^G_C$ (hand-eye) | 같음 |

**허용:** 단일 카메라 PnP 결과끼리의 **거리**.

```
카메라 i 하나만:
   비전:  |Δp| = | t_cam_cube(a) − t_cam_cube(b) |
   FK  :  |Δp| = | t_base_gripper(a) − t_base_gripper(b) |
```

단일 카메라 PnP는 **내부 파라미터 + 마커 기하 + 검출 코너**만 쓴다. 외부파라미터가 필요 없다.
그리고 **거리는 좌표계에 무관한 불변량**이라 카메라 좌표계와 base 좌표계를 정렬할 필요조차 없다.
**캘리브레이션이 개입할 자리가 없다.**

**규칙 2 — 회전 차이가 작은 쌍만 쓴다**

그리퍼 원점과 큐브 중심은 약 162 mm 떨어져 있다. 로봇이 **회전**하면 큐브 중심의 경로가 그리퍼 원점 경로와 달라진다. 이걸 보정하려면 $T^G_{cube}$가 필요한데, 그건 session1로 적합된 값이라 순환이다.

```
|Δrotation(a, b)| < 5° 인 쌍만 선택
```

회전이 거의 없으면 큐브 중심 이동 ≈ 그리퍼 원점 이동이라 $T^G_{cube}$가 **식에서 떨어져 나간다.**
45장이면 쌍이 990개이므로 조건을 걸어도 충분히 남는다.

**규칙 3 — 카메라 3대를 따로 돌린다**

| 결과 | 해석 |
|---|---|
| 세 기울기가 **일치** | **마커 스케일 문제 확정** (공통 원인) |
| 카메라마다 **다름** | 그 카메라의 **내부 파라미터** 문제 |

세 독립 측정이 같은 답을 내면 held-out보다 강한 근거가 된다.

### A.5 정밀도를 올리는 요령

PnP는 **깊이 방향이 가장 부정확하다.** 코너를 0.3 px로 잡아도 깊이 오차가 3.16 mm다(마커 50.5 mm가 65.9 px밖에 안 되기 때문). 반면 **가로세로 방향은 훨씬 정확하다.**

**마커 스케일 오차는 재구성 전체에 똑같이 곱해진다.** 그래서 정확한 가로세로 성분만 써도 스케일이 나온다.

- **주 분석:** 가로세로 성분 → 정밀한 스케일 추정
- **보조 분석:** 깊이 성분 → 깊이 왜곡 진단용

### A.6 z 편향의 정체도 같이 밝혀진다

같은 회귀를 **z 방향으로** 하면 진단이 갈린다.

| 결과 | 원인 |
|---|---|
| 기울기 ≠ 1 | **깊이 스케일 왜곡** (초점거리·마커 크기) |
| 기울기 = 1, 절편만 있음 | **상수 오프셋** → 공통 편향 문제 |
| 높이마다 잔차가 변함 | **외부파라미터가 특정 높이에만 맞음** |

현재 `METHODS_ANALYSIS.md`는 세 번째일 것이라고 **추정만** 한다. 이 실험이 **판정**한다.

### A.7 실험 A' — 그리퍼캠은 방향을 뒤집어야 한다

**중요:** 큐브를 쥐면 그리퍼캠과 큐브가 **같이 움직인다.** 둘의 상대 자세가 **상수**가 되어, 그리퍼캠 관측은 $T^G_{cam}$과 $T^G_{cube}$의 **합성만** 알려주고 둘을 분리하지 못한다. 수학적으로 퇴화(degenerate)다.

**증거가 데이터에 있다.** `pass1_grasp_offset_replayed_0914.json`의 카메라별 관측 수:

```
{'039422061216': 14, 'fixed2': 12, 'fixed3': 15, 'gripper': 0}
```

**그리퍼캠 0개.** 세 버전 전부 그렇다.

따라서 스위프는 두 종류가 필요하다.

| | 큐브 | 카메라 | 정보를 얻는 쪽 |
|---|---|---|---|
| **A (고정캠용)** | **쥐고 이동** | 고정캠 정지 | **고정캠** ✅ / 그리퍼캠 ❌ |
| **A' (그리퍼캠용)** | **놓아서 정지** | **그리퍼캠이 이동** | 그리퍼캠 ✅ / 고정캠 ❌ |

**A'가 session3 방식이다.** 큐브(또는 보드)를 고정해두고 로봇이 그리퍼캠을 여러 자세로 옮긴다. 이번엔 **카메라가 움직인 양**을 FK가 알려주고, 그리퍼캠이 본 정지 물체의 상대 변화와 비교한다.

**둘은 서로 대체 불가다.** A는 고정캠 스케일, A'는 EIH 체인 스케일을 잰다. 그리고 **A'가 우리 논문 기여(그리퍼캠)를 직접 검증하는 쪽**이다.

⚠️ session3는 15장뿐이라 자세 범위가 충분한지 확인이 필요하다. 부족하면 추가 촬영.

### A.8 산출물

| 항목 | 형태 |
|---|---|
| 카메라별 기울기 $a_i$ ± 표준오차 | 표 |
| 세 카메라 일치 여부 | 판정 |
| 확정된 마커 스케일 | **하나의 숫자** |
| z 회귀 결과 | 위 A.6 세 갈래 중 판정 |

⚠️ **스케일을 확정한 뒤에는 다시 건드리지 않는다.** 재적합 결과를 보고 또 조정하면 "물리 치수를 데이터로 튜닝" 문제로 되돌아간다. [SOTA_Claim_Protocol.md](SOTA_Claim_Protocol.md)의 frozen 원칙을 적용한다.

---

## 실험 B — flange ↔ cube 변환의 정확도

### B.1 문제

현재 $T^G_{cube}$ = **[0.43, −0.07, 162.04] mm**이고, **session1 영상으로 적합**한 값이다.

이게 문제인 이유는 두 가지다.

1. **외부 GT가 이 값을 쓴다.** GT = 로봇이 놓은 자세 × $T^G_{cube}$. 그런데 $T^G_{cube}$가 비전에서 나왔으니 **GT가 비전과 독립이 아니다.** 리뷰어 C의 치명적 지적이다.
2. **그리퍼캠이 이 적합에 0개 기여했다.** 고정캠 3대만으로 정해졌다.

**필요한 것: 비전을 안 쓰고 $T^G_{cube}$를 구하는 방법.**

### B.2 방법 B1 — 회전 궤적법 (장비 0원, 추천)

**원리:** 물체를 한 축으로 돌리면, 회전축에서 벗어난 점은 **원을 그린다.** 그 **원의 반지름이 곧 축으로부터의 거리**다.

이건 로봇 TCP 보정의 표준 원리와 같다.

**B1-a: x, y 오프셋 구하기**

```
큐브를 쥔 채, 그리퍼 z축(rz)으로만 회전   예: 0°, 45°, 90°, ..., 315°
매번 촬영 → 큐브 중심 궤적을 본다
```

| 관측 | 결론 |
|---|---|
| 큐브 중심이 **제자리에서 돌기만** 함 | x, y 오프셋 ≈ 0 |
| 큐브 중심이 **반지름 $r$의 원**을 그림 | $\sqrt{x^2+y^2} = r$ |

현재 적합값은 x = 0.43, y = −0.07이므로 $r ≈ 0.44$ mm를 예상한다. **이게 맞는지 직접 확인된다.**

**B1-b: z 오프셋(162 mm) 구하기**

```
같은 방식으로 다른 축(ry 또는 rx)으로 회전
```

이번엔 **z 오프셋이 원을 그린다.** 반지름 ≈ 162 mm다. 반지름이 크므로 **아주 정밀하게 측정된다** — 비전 잡음 0.4 mm에 비해 162 mm는 400배 크다.

**왜 이게 비전 독립에 가까운가:** 원의 반지름은 **상대량**이다. 카메라 외부파라미터도, 절대 위치도 필요 없다. 단일 카메라 PnP로 본 궤적에 원을 맞추면 된다. 실험 A와 같은 논리다.

⚠️ 완전히 비전 무관은 아니다(PnP는 씀). 하지만 **캘리브레이션 결과는 안 쓴다.** 순환이 끊긴다.

**필요한 것:** 각 축당 8~12자세, 2시간이면 충분.

### B.3 방법 B2 — 접촉법 (비전 완전 배제)

**원리:** 큐브를 쥔 채 큐브의 **한 꼭짓점**을 테이블에 고정한 뾰족한 점에 **여러 자세로** 접촉시킨다. 접촉점은 공간상 같은 한 점이므로, 여러 자세의 FK로부터 "그리퍼 기준 꼭짓점 위치"를 최소제곱으로 풀 수 있다.

이게 **4점 TCP 보정**이라 부르는 표준 절차다. **카메라를 전혀 안 쓴다.**

```
1. 테이블에 뾰족한 기준점 고정
2. 큐브 꼭짓점을 그 점에 접촉 — 자세를 바꿔가며 4~8회
3. 최소제곱 → 그리퍼 기준 꼭짓점 위치
4. 꼭짓점 → 큐브 중심은 큐브 기하로 변환 (캘리퍼스 또는 CMM 실측)
```

**장점:** 비전이 0 % 들어간다. GT 순환이 완전히 끊긴다.
**단점:** 손으로 접촉시켜야 해서 번거롭고, 접촉 판정에 사람 판단이 들어간다. 그리고 FK 오차가 섞인다.

### B.4 방법 B3 — 재파지 반복성 (정확도가 아니라 반복성)

$T^G_{cube}$가 **매번 같은가**를 본다. 정확도와는 다른 질문이다.

```
큐브를 고정된 한 자리에 둔다
반복 N = 20:
    쥔다 → 항상 같은 관측 자세로 이동 → 촬영 → 제자리에 놓는다
```

**로봇 자세가 같으므로 FK 오차와 카메라 오차가 전부 상쇄된다.** 남는 산포가 재파지 반복성이다.

**함께 필요한 대조군:** 한 번 쥔 채 **움직이지 않고** 20장 연속 촬영한다. 그 산포가 순수 측정 잡음이다.

$$\text{진짜 재파지 반복성} = \sqrt{\sigma_{\text{재파지}}^2 - \sigma_{\text{잡음}}^2}$$

**기존 데이터로 근사가 이미 가능하다.** `analyze_release_slip.py`가 내는 (held − FK 앵커)를 평균과 산포로 쪼개면 된다.

| 성분 | 값 (문서 기재) | 의미 |
|---|---|---|
| 평균 | +0.71 / +0.92 / **+2.01** mm | 카메라-FK **계통** 편향 |
| 산포 (MAD) | 0.56 / 0.81 / 0.77 mm | **재파지 반복성 + 잡음** |
| 잡음 바닥 | 0.42 / 0.43 / 0.33 mm | 카메라 간 편차 기반 |

빼면 **축당 대략 0.4~0.65 mm**다. 다만 15세트의 **자세가 전부 달라서** 자세 의존 편향이 섞인다. 그래서 B3의 자세 고정 실험이 필요하다.

### B.5 세 방법 비교

| | 비전 사용 | FK 사용 | 잼 | 시간 | 비용 |
|---|---|---|---|---|---|
| **B1 회전 궤적** | PnP만 (캘리브 무관) | ✅ | **정확도** | 2시간 | 0원 |
| **B2 접촉** | **안 씀** | ✅ | **정확도** | 반나절 | 0원 |
| **B3 재파지** | PnP만 | ✅ | **반복성** | 반나절 | 0원 |

**권장: B1 → B3 순서, 여유 있으면 B2로 교차검증.**

B1과 B2가 일치하면 $T^G_{cube}$가 확정된다. 그러면 **외부 GT의 순환이 끊어진다.** 리뷰어 C의 치명적 지적이 해소된다.

---

## 실험 C — 로봇 정밀도

### C.1 왜 필요한가 — 실험 A의 전제이기 때문

**실험 A 전체가 "로봇의 상대 이동이 정확하다"에 기대고 있다.** 이 전제가 검증되지 않으면 A의 결론도 흔들린다.

리뷰어가 반드시 물을 질문이다. *"로봇 이동량을 자로 썼다는데, 그 자가 정확한 건 어떻게 아느냐?"*

지금 우리 근거는 **데이터시트 ±0.03 mm**뿐이고, 직접 측정한 적이 없다.

### C.2 두 가지를 구분한다

| | 뜻 | 실험 A에 필요한가 |
|---|---|---|
| **반복정밀도** | 같은 자세로 돌아오는 능력 | 보조 |
| **상대 이동 정확도** | "50 mm 가"라고 했을 때 **실제 50 mm** 가는가 | **✅ 핵심** |
| 절대정확도 | 명령 좌표에 실제로 가는가 | ❌ 필요 없음 |

**우리가 재야 할 건 가운데 것이다.** 절대정확도는 실험 A와 무관하다.

### C.3 방법 C1 — 다이얼 게이지 + 게이지 블록 (정석, 추천)

**이게 상대 이동 정확도를 직접, 0.01 mm로 잰다.**

```
1. 다이얼 게이지를 테이블에 단단히 고정 (측정축 = 로봇 이동축과 평행)
2. 로봇 끝(또는 쥔 큐브)을 게이지 측정자에 접촉 → 눈금 0점
3. 로봇을 정확히 50 mm 후퇴 명령
4. 그 틈에 50 mm 게이지 블록을 끼운다
5. 로봇을 다시 접촉 → 눈금을 읽는다
6. 눈금 편차 = "50 mm 명령"의 실제 오차
```

| 항목 | 값 |
|---|---|
| 다이얼 게이지 분해능 | **0.01 mm** |
| 게이지 블록 정확도 | 교정품이면 **µm급** |
| 비전 개입 | **0 %** |

**50, 100, 150 mm 블록으로 반복**하면 이동 거리별 오차 곡선이 나온다. 축(x, y, z)별로도 하면 방향별 차이까지 보인다.

**비용:** 다이얼 게이지 + 스탠드 수만 원, 게이지 블록 세트 수십만 원. 학내 기계공작실에 보통 있다.

### C.4 방법 C2 — 반복정밀도 (비전으로, 단 한계 있음)

```
자세 A → 멀리 이동 → 다시 자세 A → 촬영.  N = 30회 반복
비전으로 본 큐브 pose의 산포를 본다
```

⚠️ **중요한 한계:** 로봇 반복정밀도(±0.03 mm)가 **비전 잡음(약 0.4 mm)보다 13배 작다.** 그래서 **비전으로는 로봇 반복정밀도를 잴 수 없다.**

**하지만 이 실험은 여전히 가치가 있다.** 해석을 바꾸면 된다.

> 비전으로 본 산포가 0.4 mm 수준에 머물면
> → 로봇 반복정밀도는 **비전 잡음에 묻힐 만큼 작다**는 확인이 된다.
> → 실험 A에서 로봇을 자로 쓰는 것이 정당화된다.

즉 **"로봇이 얼마나 정밀한가"가 아니라 "로봇 정밀도가 무시 가능한가"를 확인하는 실험**이다. 그게 실험 A에 필요한 전부다.

만약 산포가 1 mm를 넘으면 로봇 쪽 문제를 의심하고 C1로 넘어간다.

### C.5 방법 C3 — 데이터시트 확인 (5분)

Zeus 로봇의 **공식 반복정밀도 사양**을 문서에서 확인하고 출처와 함께 기록한다. 현재 `미확인` 상태다. R3 논문이 UR5/UR3에 대해 ±0.03 mm를 인용하는데, **우리 Zeus 값은 아직 확인 안 됐다.**

⚠️ 데이터시트는 **반복정밀도**만 광고한다. **절대정확도는 보통 안 적혀 있다.** 둘을 혼동하면 안 된다.

### C.6 권장

| 순서 | 방법 | 비용 | 시간 |
|---|---|---|---|
| 1 | **C3 데이터시트 확인** | 0원 | 5분 |
| 2 | **C2 비전 반복성** | 0원 | 1시간 |
| 3 | **C1 다이얼 게이지** | 수십만 원 | 반나절 |

**C3 + C2로 "로봇 정밀도는 무시 가능"이 확인되면 실험 A를 신뢰할 수 있다.**
**C1은 리뷰어가 강하게 물고 늘어질 때를 대비한 확정 근거**다. 논문에 한 줄 넣으면 그 질문이 봉쇄된다.

---

## 실험들의 의존 관계

```
C (로봇 정밀도)
   └─> A (스케일 스위프)의 전제를 검증
           ↓
        A ─> 마커 스케일 확정 → 재적합
           ↓
        B (flange↔cube) ─> 외부 GT의 순환 제거
           ↓
        최종 정확도 주장
```

**핵심: C 없이 A를 하면 "그 자가 정확하냐"는 질문에 답을 못 한다.**
**A 없이 재적합하면 스케일이 여전히 튜닝된 값이다.**
**B 없이 GT를 쓰면 순환이 남는다.**

---

## 실행 순서와 비용

| 순서 | 실험 | 재촬영 | 비용 | 시간 | 해소되는 지적 |
|---|---|---|---|---|---|
| 1 | **C3** 데이터시트 | ❌ | 0원 | 5분 | 로봇 사양 `미확인` |
| 2 | **A** session1 스위프 | ❌ **불필요** | 0원 | 반나절 | **마커 스케일 튜닝 (치명적)** |
| 3 | **B3** 재파지 근사 | ❌ **불필요** | 0원 | 2시간 | GT 반복성 미측정 |
| 4 | **C2** 비전 반복성 | ✅ | 0원 | 1시간 | A의 전제 |
| 5 | **B1** 회전 궤적 | ✅ | 0원 | 2시간 | **GT 순환 (치명적)** |
| 6 | **A'** 그리퍼캠 스위프 | ✅ 아마도 | 0원 | 반나절 | EIH 기여 검증 |
| 7 | **C1** 다이얼 게이지 | ✅ | 수십만 원 | 반나절 | 확정 근거 |
| 8 | **B2** 접촉 | ✅ | 0원 | 반나절 | 교차검증 |

**2번과 3번은 기존 데이터로 오늘 바로 가능하다.** 재촬영이 필요 없다.
**전체 합계가 수십만 원**이고, 1천만 원짜리 계측 장비가 한 대도 안 들어간다.

---

## 이 문서가 다루지 않는 것

**공통 편향(전역 6-DoF)은 상대값으로 못 잡는다.** 별도 계획이 필요하다.

| 방법 | 잡는 자유도 | FK 사용 | 비용 |
|---|---|---|---|
| 중력 (디지털 수준기) | roll, pitch | **안 씀** | 수만 원 |
| 로봇 base에 기준물 | 6개 전부 | **안 씀** | 수십만 원 |
| 포인터 접촉 | 6개 전부 | 씀 | 0원 |

세 경로가 서로 독립이므로 **교차검증**이 된다. 상세는 별도 문서로.

---

## 용어 정리

| 용어 | 쉬운 정의 |
|---|---|
| **반복정밀도** | 같은 명령을 줬을 때 매번 같은 양만큼 움직이는 능력 |
| **절대정확도** | 명령한 좌표에 실제로 정확히 가는 능력. 반복정밀도보다 훨씬 나쁘다 |
| **PnP** | 마커의 실제 크기를 알 때, 사진 속 찌그러진 모양으로 카메라↔마커 거리·각도를 역산하는 것 |
| **FK (순기구학)** | 로봇이 모터 각도만 보고 자기 손 위치를 계산하는 것 |
| **스케일 오차** | 거리를 일정 **비율**로 크거나 작게 재는 오차. 마커 크기를 잘못 알면 생긴다 |
| **공통 편향** | 모든 측정이 **같은 방향으로 함께** 틀어진 오차. 상대값으로는 못 잡는다 |
| **퇴화 (degenerate)** | 관측이 두 미지수의 **합성만** 알려줘서 각각을 분리할 수 없는 상태 |
| **held-out** | 적합에 쓰지 않고 숨겨둔 데이터로 채점하는 것 |
| **TCP** | Tool Center Point. 로봇이 "내 손끝"이라고 여기는 지점 |

---

## 한 줄 요약

**로봇에게 "정확히 100 mm만 움직여줘"라고 시키고 비전이 몇 mm라고 하는지 보면, 비싼 장비 없이 마커 스케일·그리퍼-큐브 변환·로봇 정밀도를 전부 잴 수 있다. 단 "모두가 같이 틀어진 오차" 하나만은 이 방법으로 못 잡으므로 따로 다룬다.**
